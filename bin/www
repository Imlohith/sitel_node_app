const app = require('../src/index');
require('dotenv').config()
require('../src/helpers/init_mongodb')

// Get port from environment and store in Express.
const port = process.env.PORT || '4001';

// starting the server
app.listen(port, () => {
    console.log('App liesting at the ' + port)
})